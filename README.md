# Predictive-analysis-for-WildFires
A machine learning system that predicts wildfire occurrence before it happens, using environmental data like temperature, humidity, wind speed, rainfall, and vegetation — rather than waiting for fire to start (which is the big gap in current systems).
